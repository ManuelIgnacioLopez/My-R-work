#R markdown 
install.packages("rmarkdown")

# En el archivo .Rmd (si usan Rstudio) al clickear el boton Knit se les despliega la version del archivo en html

# En rmarkdown los # se usan para  titulos de secciones, mientras mas # mas chicos son los titulos

# si se colocan <texto> el texto es cliqueable y se entiende como un link
# si queremos que haya un link que se vea como texto en vez del url, se coloca: [texto](url)

#Si colocamos texto entre * 
  # *text* se pone en italicas
  # **text** se pone en negrita
  # *texto Se usa para poner puntos para listados

#para poner una imagen se usa: ![epigrafe](url de imagen)